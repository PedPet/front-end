import { makeStyles } from "@material-ui/core";

const useFormStyles = makeStyles({
    grid: {
        marginRight: 150,
    },
    input: {
        marginBottom: 8,
    },
});

export default useFormStyles;
